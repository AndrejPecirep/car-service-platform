openapi: 3.0.0
info:
  title: Car Service Platform API
  version: 1.0.0

paths:
  /api/health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK

  /api/booking:
    post:
      summary: Create booking
      responses:
        "201":
          description: Created